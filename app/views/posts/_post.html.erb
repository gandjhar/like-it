<br>
<p><%= post.body %></p>
<p>by <%= post.user.email %></p>
<p>
   <%= link_to '[Like]', post_likes_path( post.id ), class: 'like-button', data: { post_id: post.id, user_id: current_user.id } %>
   <span id="post-likes-<%= post.id %>">
      <%= post.likes.count %>
   </span>
</p>
